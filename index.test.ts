import { Sumsub } from "./index";

describe("sumsub-typescript-sdk", () => {
    it("initialize client", async () => {
        const sumsub = new Sumsub({
            apiKey: "API_KEY",
        });
    });
});
